const api = 'api'

const apiUrl = {
    employee : {
        index:`${api}/employee`,
    },
    department: {
        index:`${api}/department`
    },
    position: {
        index:`${api}/position`
    }
}

export default apiUrl